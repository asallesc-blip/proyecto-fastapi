services:

  frontend:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: vite_frontend

    ports:
      - "5173:5173"

    volumes:
      - .:/app
      - /app/node_modules